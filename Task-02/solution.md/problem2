#Insurance

problem statement:Determine the amount that will be rebated by the insurance company.

Approach: if the repair cost y  is less than or equal to x, pay y (print y)
if the reapair cost is more than x pay x (print x)

code:
T=int(input())
for i in range(T):
    x,y=map(int,input().split())
    if y<=x:
        print(y)
    else:
        print(x)
        
